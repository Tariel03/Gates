package com.example.gates.Repositories;

import com.example.gates.Models.Done_projects;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Done_projectsRepository extends JpaRepository<Done_projects, Long> {
}